# Development
DATABASE_URL="file:./dev.db"

# NextAuth
# For production, set this to your actual domain (e.g., https://your-domain.com)
NEXTAUTH_URL="http://localhost:3000"

# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# Google OAuth (create at https://console.developers.google.com)
# For production, add your domain to authorized redirect URIs
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Admin Credentials (for email/password login - temporary credentials)
# These will be used to create the initial admin user in the database
ADMIN_TEMP_EMAIL="admin@example.com"
ADMIN_TEMP_PASSWORD="changeme"